extends layout

block content
  h1= title
  
  ul
    each paciente in paciente_list
      li 
        a(href=paciente.url) #{paciente.nome} 
        |  (#{paciente.tipo_sangue} - #{paciente.sexo} - #{paciente.dt_nascto})

    else
      li Não existem pacientes.